version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
        - cd packages/frontend
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: packages/frontend/dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
      - packages/frontend/node_modules/**/*
